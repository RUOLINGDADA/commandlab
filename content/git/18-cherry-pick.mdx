---
id: "git-18"
tool: "git"
slug: "cherry-pick"
title: "选择性移植提交"
summary: "把一个独立修复复制到当前分支，并在隔离仓库中练习、验证与安全清理。"
level: "高级"
order: 18
duration: 25
objectives: ["解释“选择性移植提交”涉及的状态变化","独立执行相关命令并验证结果"]
prerequisites: ["git-17"]
commands: [{"command":"git cherry-pick <commit>","description":"本节核心命令：选择性移植提交"},{"command":"git cherry-pick --abort","description":"用于比较作用范围与使用场景"}]
pitfall: {"symptom":"没有确认状态就执行 git cherry-pick <commit>，得到与预期不同的结果。","cause":"忽略了当前分支、工作区、暂存区或命令作用范围。","recovery":"停止继续写入，先用 git status、git log 和 git reflog 检查并保存现场。"}
comparison: {"items":["git cherry-pick <commit>","git cherry-pick --abort"],"guidance":"先判断目标是观察状态、改变工作区、移动引用还是改写历史，再选择命令。","risk":"只读检查风险低；覆盖工作区或改写共享历史前必须建立恢复点。","reversible":"Git 对象通常可从 reflog 追踪，未提交且被覆盖的内容可能无法恢复。"}
related: ["git cherry-pick <commit>","git cherry-pick --abort","git status","git help"]
quiz: {"question":"学习“选择性移植提交”时，哪项习惯最可靠？","options":["不看状态直接执行写命令","先观察、预测影响，再执行并验证","命令没有报错就表示目标完成"],"answer":1,"explanation":"可靠操作包含观察、预测、执行和验证四步。"}
practice: {"goal":"在独立仓库练习：选择性移植提交","steps":["从种子提交创建 feature/fix 分支，在该分支新增一个文件并提交；用 git log --oneline --all 找到这个真实提交哈希","切回 main 后执行 git cherry-pick <刚才复制的提交哈希>，再用 git show --stat HEAD 检查移植结果"],"expected":"main 新增一个提交，文件内容与 feature/fix 的修复一致；git status --short 为空。","hint":"尖括号只是占位符，必须替换成 git log 输出的真实哈希；冲突时先 git status，再决定继续或 git cherry-pick --abort。","solution":"只选择一个明确的修复提交进行移植，完成后比较两个分支的文件和提交说明，不要直接复制一串未知哈希。","variant":"让两个分支修改同一行，观察冲突标记，解决后执行 git add 文件名 与 git cherry-pick --continue。"}
platforms: [{"platform":"windows","setup":["New-Item -ItemType Directory -Force commandlab-git-lab | Out-Null","Set-Location commandlab-git-lab","git init","git config user.name \"CommandLab Learner\"","git config user.email \"learner@example.com\"","Set-Content note.txt 'CommandLab'","git add note.txt","git commit -m \"docs: seed lab\"","git log --oneline --all -5"],"verify":["git status --short"],"cleanup":["Set-Location ..","Remove-Item -Recurse -Force commandlab-git-lab"]},{"platform":"macos","setup":["mkdir -p commandlab-git-lab && cd commandlab-git-lab","git init","git config user.name \"CommandLab Learner\"","git config user.email \"learner@example.com\"","printf 'CommandLab\\n' > note.txt","git add note.txt","git commit -m \"docs: seed lab\"","git log --oneline --all -5"],"verify":["git status --short"],"cleanup":["cd ..","rm -rf commandlab-git-lab"]},{"platform":"linux","setup":["mkdir -p commandlab-git-lab && cd commandlab-git-lab","git init","git config user.name \"CommandLab Learner\"","git config user.email \"learner@example.com\"","printf 'CommandLab\\n' > note.txt","git add note.txt","git commit -m \"docs: seed lab\"","git log --oneline --all -5"],"verify":["git status --short"],"cleanup":["cd ..","rm -rf commandlab-git-lab"]}]
---

## 本节要解决什么

把一个独立修复复制到当前分支，并在隔离仓库中练习、验证与安全清理。先把命令要影响的对象说清楚，再执行；这样你能判断输出是否合理，而不是只看“有没有报错”。

## 先准备，再操作

1. 进入课程上方练习步骤创建的独立仓库，先运行 `git status`。
2. 对照步骤中写出的前置状态；如果当前分支、文件或远端不存在，先完成准备动作，不要跳过。
3. 执行核心命令 `git cherry-pick <commit>`，把完整输出和退出码保留下来。
4. 使用练习要求的验证命令检查结果，再解释哪一个对象发生了变化。
5. 确认没有需要保留的修改后，执行平台对应的定向清理命令。

## 命令拆解

`git cherry-pick <commit>`：把指定提交的改动复制到当前分支；目标提交必须存在且可能产生冲突。

这条命令的结果可能因 Git 版本、当前分支名和仓库内容不同而变化。稳定的验证方式是查看状态、引用或差异，而不是复制某个固定哈希或时间。

## 出错时怎么办

先停止继续输入，保留原始错误；再运行 `git status`、`git log --oneline --all` 或本节列出的只读检查，确认自己所在目录、分支和前置对象。需要改写历史的命令只在练习仓库或未共享分支中执行；不确定时先创建分支或记录当前提交：`git branch backup-before-18-cherry-pick`。

## 课后复盘

用自己的话回答：命令读取或改变了哪个对象？验证输出中哪一行证明目标达成？如果结果不对，最先检查哪个前置条件？清理时为什么只能删除本节创建的资源？
