---
id: "docker-08"
tool: "docker"
slug: "build-context"
title: "控制构建上下文"
summary: "为构建准备一个只包含应用文件的目录，验证 Dockerfile 能读取的文件边界。"
level: "进阶"
order: 8
duration: 22
objectives: ["理解控制构建上下文涉及的对象和状态变化","完成真实任务并用关键字段验证","能保留错误证据并安全清理"]
prerequisites: ["已安装 Docker Desktop 或 Docker Engine"]
scenarios: [{"id":"scenario-build-context-main","title":"控制构建上下文：主案例","context":"你正在处理一个真实的“控制构建上下文”任务，需要从准备到验证形成可交接记录。","goal":"为构建准备一个只包含应用文件的目录，验证 Dockerfile 能读取的文件边界。","prerequisites":["Docker 引擎已启动","终端可执行 docker version"],"images":[{"name":"alpine:3.20","source":"Docker Hub 官方镜像","purpose":"提供轻量、可重复的 shell 环境","risk":"仅在 commandlab- 前缀资源上操作；挂载、端口和权限仍可能影响宿主机"}],"steps":[{"id":"step-build-context-1","prompt":"你负责“控制构建上下文”场景。请在本机完成这项任务：先准备并完成主操作，留下可供复核的结果。","objective":"用一个真实开发或运维场景理解“控制构建上下文”：明确操作对象、状态变化、验证证据和清理边界。","preparation":["确认 Docker Desktop 或 Docker Engine 已启动。","只创建或操作名称以 commandlab-build-context 开头的资源；发现旧资源时先记录状态再处理。"],"hints":["先写下你预期看到的对象和状态，再执行答案中的命令。","错误出现时保留完整文本，按“对象—状态—原因—恢复”顺序排查。","ID、时间、摘要和平台可能不同，只判断关键字段。"],"answer":{"explanation":"这组操作把“控制构建上下文”放进常见工作流。第一条命令负责创建或改变目标对象，第二条命令用于观察结果；命令不会在浏览器执行。","commands":[{"command":"docker build -f docker/Dockerfile -t commandlab-context docker/","description":"完成本步骤的主操作；参数只影响本案例资源。","shell":"sh"},{"command":"docker image inspect commandlab-context --format '{{json .Config}}'","description":"紧接着观察主操作产生的对象或输出，记录关键字段。","shell":"sh"}]},"expected":"完成后应能看到本节目标对象或镜像存在，状态符合“控制构建上下文”场景；验证时只判断关键字段，不把随机 ID、具体时间或版本号作为固定答案。","verify":[{"command":"docker image inspect commandlab-context --format '{{.Config.Cmd}}'","description":"读取关键字段，证明对象确实达到目标状态。","shell":"sh"}],"cleanup":[{"command":"docker image rm commandlab-build-context 2>/dev/null || true","description":"只清理当前案例明确创建的 commandlab- 资源。","shell":"sh"}],"pitfalls":[{"symptom":"出现 name is already in use、退出码异常或对象状态与预期不同。","cause":"上一次练习留下同名资源、当前 context 不同，或把停止容器误认为已删除。","diagnosis":"先执行 docker ps -a --filter name=commandlab-build-context 和 docker inspect commandlab-build-context，记录 Names、State、Mounts 等字段；不要立即重试。","recovery":"确认只涉及 commandlab-build-context 后执行定向清理，再从创建步骤开始。"}],"comparisons":[{"command":"docker inspect","purpose":"读取结构化配置和状态","scope":"只读指定对象","risk":"低，环境变量可能出现在输出中","reversible":"完全可逆"},{"command":"docker rm","purpose":"删除容器对象","scope":"仅指定容器及其可写层","risk":"中，未挂载数据可能丢失","reversible":"容器删除不可撤销，卷通常仍可保留"}],"variants":["把镜像标签或一个非关键参数改成另一种常见配置，先预测差异再验证。","把本步骤写成一条交接记录：对象、状态、证据、清理结果。"],"dangerous":false},{"id":"step-build-context-2","prompt":"你负责“控制构建上下文”场景。请在本机完成这项任务：根据前一步结果完成变体或故障恢复，并说明状态变化。","objective":"用一个真实开发或运维场景理解“控制构建上下文”：明确操作对象、状态变化、验证证据和清理边界。","preparation":["确认 Docker Desktop 或 Docker Engine 已启动。","只创建或操作名称以 commandlab-build-context 开头的资源；发现旧资源时先记录状态再处理。"],"hints":["先写下你预期看到的对象和状态，再执行答案中的命令。","错误出现时保留完整文本，按“对象—状态—原因—恢复”顺序排查。","ID、时间、摘要和平台可能不同，只判断关键字段。"],"answer":{"explanation":"这组操作把“控制构建上下文”放进常见工作流。第一条命令负责创建或改变目标对象，第二条命令用于观察结果；命令不会在浏览器执行。","commands":[{"command":"docker image inspect commandlab-context --format '{{json .Config}}'","description":"完成本步骤的主操作；参数只影响本案例资源。","shell":"sh"},{"command":"docker image inspect commandlab-context --format '{{json .Config}}'","description":"紧接着观察主操作产生的对象或输出，记录关键字段。","shell":"sh"}]},"expected":"完成后应能看到本节目标对象或镜像存在，状态符合“控制构建上下文”场景；验证时只判断关键字段，不把随机 ID、具体时间或版本号作为固定答案。","verify":[{"command":"docker image inspect commandlab-context --format '{{.Config.Cmd}}'","description":"读取关键字段，证明对象确实达到目标状态。","shell":"sh"}],"cleanup":[{"command":"docker image rm commandlab-build-context 2>/dev/null || true","description":"只清理当前案例明确创建的 commandlab- 资源。","shell":"sh"}],"pitfalls":[{"symptom":"出现 name is already in use、退出码异常或对象状态与预期不同。","cause":"上一次练习留下同名资源、当前 context 不同，或把停止容器误认为已删除。","diagnosis":"先执行 docker ps -a --filter name=commandlab-build-context 和 docker inspect commandlab-build-context，记录 Names、State、Mounts 等字段；不要立即重试。","recovery":"确认只涉及 commandlab-build-context 后执行定向清理，再从创建步骤开始。"}],"comparisons":[{"command":"docker inspect","purpose":"读取结构化配置和状态","scope":"只读指定对象","risk":"低，环境变量可能出现在输出中","reversible":"完全可逆"},{"command":"docker rm","purpose":"删除容器对象","scope":"仅指定容器及其可写层","risk":"中，未挂载数据可能丢失","reversible":"容器删除不可撤销，卷通常仍可保留"}],"variants":["把镜像标签或一个非关键参数改成另一种常见配置，先预测差异再验证。","把本步骤写成一条交接记录：对象、状态、证据、清理结果。"],"dangerous":false}]}]
interactiveQuiz: {"question":"控制构建上下文场景中，哪项是完成验收时必须成立的判断？","options":["构建上下文决定 COPY 可见范围","可以忽略这一事实：Dockerfile 路径和上下文路径是两个概念","发生错误时无需验证，因为把项目根目录当上下文可能上传无关大文件"],"answer":0,"explanation":"该判断直接对应“控制构建上下文”的关键对象和验收证据；其他选项会把不同对象或状态混为一谈。","type":"choice"}
platforms: [{"platform":"windows","notes":"Windows 建议使用 WSL2；PowerShell 命令与 POSIX 挂载写法按代码块标注选择。","setup":[{"command":"docker version","description":"确认客户端与服务端可连接","shell":"sh"}],"verify":[{"command":"docker ps -a --filter name=commandlab-","description":"列出本课程资源并核对状态","shell":"sh"}],"cleanup":[{"command":"docker rm -f $(docker ps -aq --filter name=commandlab-) 2>/dev/null || true","description":"只清理 commandlab- 前缀容器","shell":"sh"}]},{"platform":"macos","notes":"macOS 使用 Docker Desktop，挂载目录前确认已授权。","setup":[{"command":"docker version","description":"确认客户端与服务端可连接","shell":"sh"}],"verify":[{"command":"docker ps -a --filter name=commandlab-","description":"列出本课程资源并核对状态","shell":"sh"}],"cleanup":[{"command":"docker rm -f $(docker ps -aq --filter name=commandlab-) 2>/dev/null || true","description":"只清理 commandlab- 前缀容器","shell":"sh"}]},{"platform":"linux","notes":"Linux 直接连接 Engine；权限错误时先检查当前用户和 Docker context。","setup":[{"command":"docker version","description":"确认客户端与服务端可连接","shell":"sh"}],"verify":[{"command":"docker ps -a --filter name=commandlab-","description":"列出本课程资源并核对状态","shell":"sh"}],"cleanup":[{"command":"docker rm -f $(docker ps -aq --filter name=commandlab-) 2>/dev/null || true","description":"只清理 commandlab- 前缀容器","shell":"sh"}]}]
---

## 场景说明

为构建准备一个只包含应用文件的目录，验证 Dockerfile 能读取的文件边界。

## 关键概念

本节只讲完成任务必须的常用操作。先完成实操题，再展开答案；随机 ID、时间和镜像摘要只作为现场证据，不作为固定答案。

## 课后复盘

完成后写下：本节命令操作的是镜像、容器、卷、网络还是构建缓存？是否改变宿主机文件？再次执行会复用什么、重新创建什么？
