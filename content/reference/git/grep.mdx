---
id: "git-ref-grep"
tool: "git"
slug: "grep"
title: "git grep"
category: "搜索与排错"
object: "工作区或提交中的文本"
summary: "在受 Git 管理的文件中搜索文本，并可限定到某个提交。"
usage: "定位配置名、错误信息或某个字符串首次出现的位置。"
syntax: "git grep [--cached|<commit>] [-n] <pattern> [-- <path>...]"
parameters:
  - {flag: "-n", description: "显示每个匹配结果所在的行号。"}
  - {flag: "--cached", description: "搜索暂存区内容，而不是当前工作区。"}
  - {flag: "<commit>", description: "搜索指定提交快照中的内容。"}
commonOptions: ["-n：显示行号", "--cached：搜索暂存区", "-i：忽略大小写"]
scenarios: ["在当前项目中查找一个环境变量名。", "比较某个旧提交是否仍包含已删除的配置。"]
comparisons: ["与系统 grep 对比：git grep 默认遵循 Git 跟踪范围，可直接搜索提交对象。"]
risk: "低"
reversible: "命令只读，不会改变仓库对象或工作区内容。"
errors: ["返回码 1：没有匹配项，不一定表示命令失败；先确认搜索词和路径。"]
examples: [{command: "git grep -n TODO -- '*.md'", description: "查找 Markdown 文件中的 TODO", shell: "sh"}]
relatedLessons: ["git-05", "git-06", "git-14"]
---

git grep 的退出码 1 通常表示“没有找到”，不是程序崩溃。需要把 shell 的错误处理和搜索结果分开判断，并用 -- 分隔选项与路径，避免路径名被误当成参数。
