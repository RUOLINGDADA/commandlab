---
id: "git-ref-restore"
tool: "git"
slug: "restore"
title: "git restore"
category: "撤销修改"
object: "工作区或暂存区"
summary: "撤销尚未提交的文件修改，或把文件移出暂存区。"
usage: "确认文件不需要后再撤销，避免覆盖唯一一份未提交内容。"
syntax: "git restore [--staged] [--source=<tree>] [--] <path>"
parameters:
  - {flag: "--staged", description: "只撤回暂存，不改变工作区文件。"}
  - {flag: "--source=<tree>", description: "从指定提交或分支恢复文件内容。", example: "--source=HEAD"}
  - {flag: "-- path", description: "限定要恢复的文件路径。"}
commonOptions: ["--staged：移出暂存区", "--source：指定恢复来源"]
scenarios: ["撤回误执行的 git add。", "放弃一个文件的本地未提交修改。"]
comparisons: ["与 git reset 对比：restore 专注文件内容和暂存区，reset 还会移动分支指针。"]
risk: "高"
reversible: "暂存撤回通常可重新 add；覆盖工作区前若没有提交或备份，内容可能无法恢复。"
errors: ["pathspec did not match：路径拼写错误或文件不在当前提交。"]
examples: [{command: "git restore --staged README.md", description: "撤回 README 的暂存", shell: "sh"}]
relatedLessons: ["git-03", "git-13"]
---

看到 `git restore 文件` 时要停一下：它可能直接覆盖工作区文件。小白先用 `git diff 文件` 看内容，再决定是否执行；只想撤销暂存则加 `--staged`。
