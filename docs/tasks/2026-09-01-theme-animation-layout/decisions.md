# 设计决策

- 动画入口改用 Next `Link`，保持站内客户端导航，让根布局和已选主题状态连续；主题仍由本地存储持久化。
- 筛选器采用可换行的 flex 布局和统一 `gap`，窄屏优先保证文字可读，再允许横向滚动作为内容过多时的兜底。
- 内嵌动画舞台通过 `aspect-ratio`、`min-width: 0` 和可滚动容器适配宽度，不改变动画数据或播放状态机。
- 内嵌 Git/Docker 舞台采用单列堆叠，避免完整动画页的固定最小列宽撑破窄卡片；Git 提交图只在自身滚动容器内保留宽画布。
