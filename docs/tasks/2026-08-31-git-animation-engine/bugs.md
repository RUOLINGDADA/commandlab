# 缺陷记录

| 状态   | 现象                                                           | 根因                                                        | 修复/验证                                                                                         |
| ------ | -------------------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------------------------------------- |
| 已修复 | 当前动画只有按 kind 套模板的装饰节点                           | 没有状态机和前后状态快照                                    | 新增 Git reducer、TeachingFrame 快照和 SVG 提交图；20 项测试与关键页面验收通过                    |
| 已修复 | 命令输入、画布变化和说明不同步                                 | interval 只切换文字帧                                       | 统一六阶段时间轴，终端、事件、SVG 共用当前帧；暂停/逐步/重播浏览器验收通过                        |
| 已修复 | includeFeature 场景默认 HEAD 在 feature，merge/switch 语义失真 | 初始状态没有区分当前分支                                    | 增加 headBranch 选项，merge 从 main 合并 feature，switch 真正移动 HEAD                            |
| 已修复 | commit 场景暂存区为空，无法生成提交                            | 场景初始快照未准备 staged 文件                              | 增加 includeStaged 初始选项并覆盖 commit 回归测试                                                 |
| 已修复 | GitHub Pages 构建曾复用普通 Turbo 缓存                         | GITHUB_PAGES 未参与缓存键                                   | 发布验收使用 `TURBO_FORCE=true` 强制重建，11 页前缀校验通过                                       |
| 已修复 | 工作区与提交图区域过窄，状态变化不醒目                         | 动画详情页右侧固定百科栏挤压主舞台，SVG 使用 980×430 小视图 | 改为全宽 IDE 式工作台，SVG 扩大到 1180×620，增加前后状态摘要与动作路径；桌面/390px 浏览器验收通过 |
| 已修复 | 默认动画推进过快，用户来不及观察                               | 时间轴固定 interval 且没有速度控制                          | 默认 0.7x，提供 0.5x/0.7x/1x，仍支持暂停、逐步和重播                                              |
| 已修复 | 执行阶段看不到节点真正生成，状态一次跳到最终结果               | transitioning 帧直接复用 finalState                         | 新增 createGitTransitionState，中间帧分离对象生成与引用移动；commit/merge/rebase 浏览器验收通过   |
| 已修复 | Docker 命令只显示生命周期颜色，缺少前后状态解释                | 画布没有比较 initialState 与当前快照                        | 新增 Docker 镜像/容器/网络/卷/端口变化摘要，并调整行距避免与摘要重叠                              |
