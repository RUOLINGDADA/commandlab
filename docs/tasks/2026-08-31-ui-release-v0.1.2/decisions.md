# 决策记录

## 版本号使用 `0.1.2`

UI、文案和 Docker 部署配置属于向后兼容的功能与交付改进，沿用当前 `0.1.x` 系列并递增补丁版本，满足发布工作流对根项目版本的校验。

## 先合并 PR 再创建 Release

仓库启用 `main` 保护和必需 `quality` 检查。Release 工作流只允许从 `main` 运行，因此先通过 PR 合并，再使用工作流创建标签和 Release，确保 Pages 使用最终提交。
