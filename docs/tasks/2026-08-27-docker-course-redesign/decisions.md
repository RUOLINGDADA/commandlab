# 决策记录

- 课程 Schema 采用新结构，不保留旧的单一 `practice` 与 `quiz` 兼容字段，避免重复模型。
- 课程 URL、编号和主题顺序保持不变，确保已发布 Pages 链接继续有效。
- 实操命令统一使用 `commandlab-` 前缀；清理命令只针对课程创建的资源。
- 预期输出只描述关键字段和判断条件，避免绑定 Docker 版本、架构或操作系统差异。
- 镜像按场景选择，优先官方镜像；使用可信第三方镜像时必须写明来源与风险。
