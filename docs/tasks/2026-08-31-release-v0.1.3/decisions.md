# 决策记录

## 版本号使用 `0.1.3`

- `0.1.2` 已是上一版已发布版本；当前交互式 Git 工作台和远端同步属于新的功能增量，因此使用下一个补丁版本 `0.1.3`。

## 通过现有 Release 工作流发布

- `.github/workflows/release.yml` 已包含版本格式校验、质量检查和 GitHub Release 创建逻辑；沿用该流程可保持 Pages 自动触发和仓库发布约束一致。
