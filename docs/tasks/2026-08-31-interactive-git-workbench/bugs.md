# Bug 记录

| 状态   | 现象                                               | 复现/修复                                                               | 回归验证                                 |
| ------ | -------------------------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------- |
| 已修复 | 点击提交节点生成的 `git show D3` 把 `git` 当成引用 | `readShow`/`readRevParse` 从 token 2 开始解析引用，避免命令前缀参与查找 | 浏览器点击提交节点后终端显示 `commit D3` |
