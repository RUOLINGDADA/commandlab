# 决策记录

- 使用浏览器内存状态而不是真实 shell：项目是静态 Next.js 导出，且在线沙箱边界明确要求独立隔离 API；本次只实现可验证的前端仿真。
- 交互执行器复用 `applyGitCommand` 的状态语义，并在会话层补齐命令解析、历史、输出和点击动作，避免复制第二套提交图模型。
- 工作区按 Source Control、Git Graph、终端三块组织；面板里的点击动作统一转为只读命令，便于用户观察命令与状态的关系。
