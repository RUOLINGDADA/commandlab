# 交接

任务正在实施。

关键文件：`apps/web/src/lib/interactive-docker.ts`、`apps/web/src/components/interactive-docker-workbench.tsx`、`apps/web/src/app/courses/[tool]/page.tsx` 和 `apps/web/src/styles/components.css`。

目标结果：Docker 学习路径顶部提供完整浏览器内仿真工作台，命令、对象面板和状态摘要共享一个确定性内存会话。
