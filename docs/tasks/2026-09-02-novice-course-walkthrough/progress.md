# 进度

## 2026-09-02

- 从 `/courses/docker/` 进入第一节课程，检查课程导航、嵌入动画、提示、答案、互动题和步骤状态。
- 发现多个嵌入动画同时自动播放，首次进入缺少单一的下一步指引。
- 发现步骤强调本机 Docker 实操，但浏览器内 Docker 工作台没有在步骤附近提供直接入口；新手若未安装 Docker 容易卡住。
- 发现动画准备阶段输出大量 Engine/对象术语，但没有先说明“观察命令是否成功”和“只读、不修改电脑”的关系。
- 嵌入动画默认暂停，并在卡片标题中标出“先看命令，再点播放”，避免多个播放器同时抢夺注意力。
- Docker 步骤增加“没有本机 Docker 也能继续”引导和步骤级浏览器仿真入口；在线终端支持 `?mode=docker#docker-terminal` 直达 Docker 模式。
- 课程底部紧凑终端根据课程工具显示 Docker 预览，不再在 Docker 课程中误显示 Git 工作台。
- 回归测试扩展到 48 项；390px 浏览器复走第一节课程、点击动画播放和步骤入口均通过。
- `pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm build` 通过；内容校验通过。
