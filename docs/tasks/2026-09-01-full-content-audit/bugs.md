# 问题记录

| 状态  | 现象                                                                           | 复现/影响                                                | 根因                                                           | 修复与回归                                                                      |
| ----- | ------------------------------------------------------------------------------ | -------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------- |
| fixed | Git 高级课程在只有一个种子提交时直接要求 rebase -i、cherry-pick、bisect 等操作 | 新手会遇到提交不存在、分支不存在或无法开始 bisect 的错误 | 通用模板没有把命令所需的历史、分支、远端和测试脚本前置写进练习 | 已补充真实前置步骤、占位符替换说明和 abort/reset 恢复路径；内容测试覆盖正文结构 |
| fixed | Docker 多数步骤把观察命令也写成“主操作：创建、启动、构建”                      | 命令作用与页面说明矛盾，初学者无法判断是否会改变资源     | 答案命令描述复用过度通用的模板文案                             | 统一改为先确认对象类型和步骤目标；新增回归测试确保该模板不再出现                |
| fixed | 命令百科缺少 clone/show/mv/rm/grep 与 Docker 定向清理/磁盘诊断条目             | 高频查询只能跳到相邻命令，缺少安全边界和验证示例         | 注册表和内容目录没有这些条目                                   | 新增 8 条百科并接入 schema、动画和教学场景；验证 64 条百科全部通过              |
