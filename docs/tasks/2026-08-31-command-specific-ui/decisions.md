# 关键决策

- 只共享播放控制、时间轴、状态快照和基础 SVG 对象；不共享完整命令舞台布局。
- 以 `tool/slug` 作为专属视图注册键，命令新增时只需增加注册项和视图函数。
- Git 工作区采用左侧 Source Control 栏、中央 Git Graph、顶部分支上下文的三层信息架构。
- 对同一命令类别可共享原子绘制函数，但每个 slug 必须有不同的布局配置、标题、重点对象或状态面板。
- 不执行真实 Git/Docker 命令，不引入第三方动画依赖。
