# 缺陷记录

| 状态   | 现象                                     | 根因                                                       | 修复/验证                                                                                                   |
| ------ | ---------------------------------------- | ---------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| 已修复 | 所有命令动画看起来像同一套 UI            | `CommandAnimation` 内部固定使用 `GitCanvas`/`DockerCanvas` | `CommandSpecificStage` 按 tool/slug 注册专属布局，浏览器 DOM 检查确认 add、branch、merge、rebase 等布局不同 |
| 已修复 | Git 工作区缺少当前分支和提交历史侧栏     | 画布只显示文件面板和提交节点                               | 增加 VS Code Source Control + Git Graph 信息架构，桌面截图与移动端宽度检查通过                              |
| 已修复 | 移动端 Source Control 被网格轨道挤成窄条 | 桌面端两栏轨道规则在移动断点后重新覆盖了堆叠规则           | 在基础规则之后追加移动断点覆盖，Source Control 与主舞台均恢复 100% 宽度                                     |
