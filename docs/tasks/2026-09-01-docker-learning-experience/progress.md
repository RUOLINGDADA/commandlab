# 进度

## 2026-09-01

- 读取项目工作流、状态、交接及既有 Docker 工作台档案。
- 确认 Docker 工作台已存在但在线终端页面仍只渲染 Git；Docker 动画时间轴使用通用五阶段输出；课程嵌入使用完整播放器布局。
- 新增统一在线终端模式切换，`/terminal/` 可在 Git 与 Docker 工作台之间切换，两个模式保留独立内存状态。
- 增强 Docker 仿真命令输出：容器端口、镜像层历史、日志时间戳、健康状态、网络/挂载、资源指标和 Compose 配置均可观察。
- 增强 Docker 动画阶段终端行，显示解析进度、对象计数、端口快照和下一步诊断命令。
- 课程嵌入播放器启用 `embedded` 紧凑模式，压缩终端、Docker 舞台、Git 图表和时间轴的重复留白。
- 回归测试扩展到 46 项；1280px 与 390px 浏览器验收确认在线终端无横向溢出、模式切换可用、Docker 课程嵌入卡片不重叠。
- `pnpm test`、`pnpm typecheck`、`pnpm lint`、`pnpm format:check`、`pnpm build` 均通过。
